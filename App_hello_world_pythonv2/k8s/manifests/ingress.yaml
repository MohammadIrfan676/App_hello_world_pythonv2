# Ingress resource for the application
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-python-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: hello-python-app.local
    http:
      paths: 
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-python-app
            port:
              number: 80